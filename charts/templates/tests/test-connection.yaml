apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mynginxhelmchart.fullname" . }}-test-connection"
  labels:
    {{- include "mynginxhelmchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mynginxhelmchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
