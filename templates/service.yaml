apiVersion: v1
kind: Service
metadata:
  name: {{ include "mynginxhelmchart.fullname" . }}
  labels:
    {{- include "mynginxhelmchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mynginxhelmchart.selectorLabels" . | nindent 4 }}
